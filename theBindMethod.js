// Bind Method
// the bind also allows us to set the this keyword,, the only difference is that the bind method does not call
// the function immediately

const eurowings = {
  airline: 'Eurowings',
  iatacode: 'EW',
  bookings: [],
};
